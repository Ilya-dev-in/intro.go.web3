{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "Webapp build",
        "command": "npm run build",
        "options": {
          "cwd": "${workspaceFolder}\\wepapp"
        }
      }
    ]
  }